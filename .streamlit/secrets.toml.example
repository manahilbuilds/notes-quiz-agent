# On Streamlit Cloud: paste this (with your real key) into
#   App  ->  Settings  ->  Secrets
# For LOCAL runs you can instead copy this to .streamlit/secrets.toml
# Either is optional — the app falls back to offline question generation.

GOOGLE_GENAI_API_KEY = "your_key_here"
